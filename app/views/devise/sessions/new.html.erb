<section class="login-signup-container">
<%= render "devise/shared/back_to_home" %>

<div class="main-cont-logsign">
  <div class="heading-text">

    <h1>Log In</h1>
  </div>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>


<div class="column main-login">

      <%= f.email_field :email, placeholder: "Email*",autofocus: true, autocomplete: "email", class: "input-default" %>
  
      <%= f.password_field :password, placeholder: "Password*", autocomplete: "current-password", class: "input-default" %>

    <div class="login-btn-container ">

      <%= f.submit "Log in", class:"btn-bg-transparent" %>
    <% end %>
    
    <%= render "devise/shared/links", class: "btn-text" %>
    </div>
</div>  
</div>
</section>

